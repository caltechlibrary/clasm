
# Action items

## Bugs and issues

- [x] Configure clasm edits (RDM Postgres config, backup directories, Origin tag) silently required a full clasm restart to take effect elsewhere in the same running process -- `RunConfigureMenu` loads/saves its own independent copy of `~/.clasm`, but `main()`'s own `cfg` (what every other domain's closures actually read) was never reloaded. Found 2026-08-18 live-testing Restore SQL Backup: saved an `rdm_postgres_config` rule via Configure clasm, confirmed it landed on disk, but the still-running process kept using the old value. Fixed same day: the `Configuration` domain closure reloads `cfg` from disk immediately after `RunConfigureMenu` returns. No dedicated test (pure `main()` wiring, no test harness for `cmd/clasm`); verified via the live repro. See DECISIONS.md, "Real bug: Configure clasm edits didn't take effect until restart...", PLAN.md Phase 20.58.
- [x] Instance/AMI creation writes the `Project` tag key capitalized (`"Project"`), but the fleet convention (reverted 2026-07-29 once every existing resource was retagged) is lowercase `"project"`, which is all `inventory.tagValues` matches -- every instance/AMI clasm creates since that reversion has had a `Project` tag its own code can never read back, silently falling through to the `Name`-tag fallback in Run SQL Backup/Archive OpenSearch Snapshot. Found 2026-08-18 while preparing to launch a dev/restore-test instance. Fixed same day: all three creation call sites (`launch_instance.go`, `launch_from_cloud_init.go`, `create_ami_from_instance.go`) now write lowercase `"project"`. Test-first, `go build`/`vet`/`test -race`/`gofmt` all clean. See DECISIONS.md, "Real bug: instance/AMI creation wrote the Project tag key capitalized, the fleet convention is lowercase", PLAN.md Phase 20.57.
- [x] It turns out having the "Run SQL Backups" automatically go to Archive SQL Backups is confusing. It should only generate the SQL backup .gz file and return to the backups menu. Rather than have the label "Run SQL Backups", it should be "Generate SQL Backup" -- fixed 2026-08-18: the auto-chain is removed entirely (the action now returns immediately after a successful dump) and the menu label is "Generate SQL Backup". Test-first, `go build`/`vet`/`test -race`/`gofmt` all clean. Not yet real-AWS-verified. See DESIGN.md, "Run SQL Backup: Drop the Archive-SQL Auto-Chain, Rename to 'Generate SQL Backup'", DECISIONS.md same title, PLAN.md Phase 20.54.
- [x] When I try to update the cloud-init.yaml for the granian-rdm-v14-test launch template I get an error about the user data being too large, this should have been fixed by gzipping I think -- root cause: gzip alone isn't always enough for large files, and there was no pre-flight size check anywhere. Fixed 2026-07-28: `encodeUserData` now uses `gzip.BestCompression` and hard-errors before any AWS call if the compressed payload still exceeds the 16384-byte limit, stating the byte count and overage. Designed, implemented, and unit-tested. See DESIGN.md, "User-Data Pre-Flight Size Check", DECISIONS.md, "User-data pre-flight size check: hard error, no remediation loop-back; switch to gzip.BestCompression", PLAN.md Phase 20.44. Real-AWS-verified 2026-07-28.
- [x] Menu-loop output-visibility bug -- fixed, real-AWS-verified, released in v0.0.4. See DECISIONS.md, "Pause for acknowledgment before every menu-loop redraw," PLAN.md Phase 20.32.
- [x] SSM never came online for the resize-verification test instances (2026-07-22) -- root cause (no SSM-capable instance profile available at all) closed structurally by the SSM-enforcement work below. `growRootFilesystem`'s own OS-level `growpart`/`resize2fs` automation still hasn't been specifically re-verified end-to-end with a proper instance profile in place -- worth doing next time Resize Instance's Root Volume comes up.
- [x] `InvalidUserData.Malformed: User data is limited to 16384 bytes` -- fixed and real-AWS-verified 2026-07-22 (`test-clasm-granian` launch template + instance). See DECISIONS.md, "Always gzip-compress user-data before base64-encoding it," PLAN.md Phase 20.34. Not yet released -- part of v0.0.5.
- [x] When selecting a cloud init, there is no abvious exit, "q" is treated as a filename -- fixed 2026-07-28: `promptCloudInitYAMLFile` now treats a bare "q" as cancel (label hints "(q to cancel)"), matching every Select-based picker's own convention. Tracing this also surfaced a second, more serious bug in the same code path: `SyncLaunchTemplate` never converted a cancellation to a clean return, so cancelling anywhere in "Sync cloud-init YAML to a launch template" exited the entire clasm program instead of returning to the Compute menu -- also fixed. Designed, implemented, unit-tested, and verified in a real interactive terminal 2026-07-28. See DESIGN.md, "Cloud-Init Picker: Discoverable Cancel + a Second, Related Bug", DECISIONS.md, "Cloud-init picker: a 'q' cancel sentinel, and a second bug found while tracing it", PLAN.md Phase 20.45.
- [x] CMTools and Pandoc should be required software when compiling to install -- fixed 2026-07-28: `codemeta.json`'s `softwareRequirements` now lists both `CMTools >= 0.0.46` and `Pandoc >= 3.9` (Pandoc moved out of `softwareSuggestions`) -- confirmed via the Makefile that both `cmt` and `pandoc` are invoked unconditionally by `make build`/`make man`, not optional. `GNU Make >= 3.8` stays a suggestion (only needed to invoke `make` itself, not to build the plain Go binary via `go build`).

## Requested features

- [ ] When a new instance has been created the success results should show the `aws ssm` terminal command in addition to the example SSH command
- [x] Produce installable binaries via `make release` -- already done: `release.bash`/`release.ps1` (added 2026-06-30) are wired into the Makefile's `release` target, which builds cross-platform dist zips (Linux x86_64/aarch64/armv7l, macOS x86_64/arm64, Windows x86_64/arm64) and points at `release.bash` to publish.
- [x] Need to be able to modify a launch templates' size and EBS storage from clasm in addition to syncing the cloud-init -- fixed 2026-07-28: new "Modify launch template's instance type / EBS root volume size" action creates a new version with both overrides (never auto-promotes, matching Sync). Expanded mid-design after a concrete example (switching `granian-rdm-v14-test` from Graviton/arm64 to x86_64) showed this also needs to swap the base AMI when the new instance type's architecture doesn't match -- now handled via a post-hoc architecture check that prompts for a new, compatible AMI only when needed. Designed, implemented, and unit-tested. See DESIGN.md, "Modify Launch Template Size", DECISIONS.md, "Modify Launch Template Size: one combined action, unfiltered instance-type picker, shrink to the AMI's snapshot floor", PLAN.md Phase 20.46. Real-AWS-verified 2026-07-28.
- [x] When spinning up a new instance successfully the IP addresses and SSH ec2 user is shown, what would be good to also show is the SSH command to SSH in using the keys created and associated with the instance similar to how via the Web UI you can look up the connection command for connecting with SSH. -- fixed 2026-07-28: the ssh command now includes `-i <key path>`, guessed from where clasm itself saves a key pair's private key (`~/.ssh/<name>.pem`) and shown only if that file actually exists; the login username is now guessed as "ubuntu" for Canonical-owned AMIs (this tool's own curated/official AMI list) instead of always hardcoding "ec2-user". Designed, implemented, and unit-tested. See DESIGN.md, "SSH Connection Info: Key Path + Username Guess", DECISIONS.md, "SSH connection info: guess the key path (only if it exists on disk) and the login username", PLAN.md Phase 20.47. Real-AWS-verified 2026-07-28.
- [x] SSM-capable instance profile enforcement at launch + associate/replace retrofit for running instances -- designed, implemented, unit-tested, and real-AWS-verified 2026-07-22. See DESIGN.md/DECISIONS.md, "SSM-Capable Instance Profile Enforcement + Retrofit", PLAN.md Phase 20.33. Not yet released -- part of v0.0.5.
- [x] arm64/aarch64 (Graviton) support + Ubuntu 26.04 LTS in the curated AMI/instance-type lists -- designed, implemented, unit-tested, and real-AWS-verified 2026-07-22. See DESIGN.md/DECISIONS.md, "ARM64 (Graviton) Support + Ubuntu 26.04 LTS", PLAN.md Phase 20.35. Not yet released -- part of v0.0.5.

**v0.0.5 release: Phases 20.33/20.34/20.35 and now IAM Profile & Role Management (20.36-20.40) are all done and real-AWS-verified -- release-ready, not yet released.** 2026-07-23, the user chose to bundle IAM Profile & Role Management into this same release rather than ship it separately as v0.0.6. See DECISIONS.md, "IAM Profile & Role Management: seven scoping decisions, bundled into v0.0.5."

- [x] IAM Profile & Role Management -- discovery (Roles/Instance Profiles/Policies, each row showing its `Origin` tag's literal value or "(unset)", sorted by recency), a config-driven `Origin` tag (key + which value means DLD-owned, both configurable in `~/.clasm`, left unset until the user's group reacts to a demo -- no hardcoded vocabulary), a read-only guard on permission-changing actions for resources not recognized as DLD-owned (tagging itself is always allowed, for support-contact purposes, on IMSS- or AWS-owned resources too), IAM resources becoming taggable via the Tag Management domain (no separate backfill action needed), a detail view (trust policy, attached policies, cross-referenced usage), curated per-use-case role/policy creation templates (Static Website, RDM Repository, Bridge Service, Patron-Facing Service, Data Processing), and CRUD completion for DLD-owned roles (Delete Role, Attach/Detach Policy). See `aim_management_and_support_proposal.md`, DESIGN.md "IAM Profile & Role Management Domain" and "CRUD completion for DLD-owned roles", DECISIONS.md "IAM Profile & Role Management: seven scoping decisions, bundled into v0.0.5", "...Origin tag revision...", and "...support CRUD for DLD-owned roles", PLAN.md Phases 20.36-20.40. Part of v0.0.5.
  - [x] Phase 20.36 (discovery, `Origin` tag config, IAM domain menu, `RequireDLDOwned` guard predicate) implemented, unit-tested, and real-AWS-verified 2026-07-23. Real bug found via live use the same day and fixed -- `ListRoles`/`ListInstanceProfiles`/`ListPolicies` don't return `Tags` inline (contrary to the original design's assumption); fixed via per-resource `ListRoleTags`/`ListInstanceProfileTags`/`ListPolicyTags` calls. See DECISIONS.md, "Real bug: ListRoles/ListInstanceProfiles/ListPolicies don't return tags inline." User confirmed the fix and all six suggested manual test items pass.
  - [x] Phase 20.37 (Tag Management domain extension for IAM resources -- Role/Instance Profile/Policy all taggable via TagRole/TagInstanceProfile/TagPolicy and their Untag counterparts, plus "Show all tags") implemented, unit-tested, and real-AWS-verified 2026-07-23 (add/update/remove on a role, instance profile, and policy; Show all tags for all three; editing `Origin` itself through the ordinary tag flow). Second real bug found via the same live testing and fixed same day -- the IAM Role/Instance Profile/Policy pickers were slow to open (sequential per-resource tag fetches); fixed by parallelizing with a bounded worker pool. See DECISIONS.md, "Parallelize per-resource IAM tag fetches." User confirmed both the tagging behavior and the speed fix.
  - [x] Phase 20.38 (IAM detail view -- View Role Detail: trust policy, attached/inline policies with on-demand document viewing, tags, SSM-capable, referenced-by-profiles; View Instance Profile Detail: tags, contained role(s), each role's SSM-capable status) implemented, unit-tested, and real-AWS-verified 2026-07-23. The "which running instance is using this profile" cross-reference was deferred per the user's own scoping call (no direct API filter exists for it).
  - [x] Phase 20.39 (curated per-use-case role/policy creation templates -- Static Website, RDM Repository Instance, Bridge Service, Patron-Facing Service, Data Processing; guided flow: pick template, supply ARN params, confirm, create role+policy, attach, auto-tag if `origin_tag.dld_value` is configured) implemented and unit-tested 2026-07-23, test-first end to end. Real-AWS testing in progress -- RDM Repository Instance confirmed fully, Static Website confirmed in read-only mode; a real usability bug (hand-typed ARNs) found and fixed same day, see DECISIONS.md, "Phase 20.39 templates collect resource names/IDs, not ARNs."
  - [x] Phase 20.40 (CRUD completion for DLD-owned roles -- Delete Role with type-to-confirm and dedicated-policy cascade-delete; Attach Policy to Role and Detach Policy from Role with a plain confirm) implemented, unit-tested, and real-AWS-verified 2026-07-23, test-first throughout. Delete Role successfully removed all three real test roles left over from Phase 20.39's template testing (`test-rdm-repo-role`, `test-static-site-role`, `test-static-site-role-2`), including their dedicated policies -- independently confirmed gone via `aws iam get-role`/`get-policy`. **All five IAM phases (20.36-20.40) now implemented, unit-tested, and real-AWS-verified.**
  - [x] Third real bug found via live testing 2026-07-23, while walking through Delete Role's real-AWS steps: `ListRoles`/`ListInstanceProfiles`/`ListPolicies` silently truncate past 100 items with no pagination handling -- three correctly-tagged test roles were missing from the Delete Role picker because this account has 121 roles. Fixed via `listAllRoles`/`listAllInstanceProfiles`/`listAllPolicies` paging on `Marker`/`IsTruncated`, test-first. Real-AWS-verifying the fix immediately surfaced a follow-on bug -- fetching all 121 roles' tags at concurrency 10 reliably throttled -- fixed by lowering concurrency to 4 and raising the IAM client's retry ceiling to 8. Re-verified against real AWS: all 121 roles fetched, all 5 DLD-owned roles (including the three test roles) correctly recognized, no throttling. See DECISIONS.md, "Real bug: ListRoles/ListInstanceProfiles/ListPolicies silently truncate past 100 items."
- [x] Set the root EBS volume size when creating an instance/launch template (instead of always inheriting the AMI's default, e.g. 8GB), and resize a running instance's root volume after the fact.
  - Designed and scoped 2026-07-21 -- see DESIGN.md, "Configurable EBS Root Volume Size", DECISIONS.md, "Configurable EBS root volume size: scope, flow coverage, and resize automation depth", and PLAN.md Phase 20.31.
  - Implemented and unit-tested 2026-07-21, both parts: setting the size at creation (instances and launch templates) and the new "Resize instance's root volume" menu entry, including automated OS-level `growpart`/`resize2fs`/`xfs_growfs` via SSM with a manual-instructions fallback for any layout it doesn't recognize (e.g. LVM). Not yet verified against real AWS.
- [x] Show instance detail / Show AMI detail (Show Launch Template already covers the launch-template half, since 2026-07-20). Designed, implemented, and unit-tested 2026-07-24, targeted for v0.0.5. Not yet real-AWS-verified. See DESIGN.md, "Instance/AMI Detail Views", DECISIONS.md, "Instance/AMI Detail Views: on-demand describe calls, appended menu placement", PLAN.md Phase 20.41.
- [x] Configure clasm domain -- a sixth top-level menu for viewing/creating/updating `~/.clasm` (regions, backup directory rules, Origin tag config) from within clasm instead of hand-editing YAML. Designed, implemented, and unit-tested 2026-07-24, targeted for v0.0.5. See DESIGN.md, "Configure clasm Domain", DECISIONS.md, "Configure clasm domain: explicit Save, region changes deferred to next launch", PLAN.md Phase 20.42.
- [ ] RDM Backup & Restore domain -- a 7th top-level menu consolidating Run SQL Backup, SQL archive (Feature 11, relocated out of Compute), OpenSearch archive, SQL restore, and OpenSearch restore. Designed end-to-end 2026-07-28 (DESIGN.md addendum, 10 DECISIONS.md entries, PLAN.md Phases 20.48-20.51), extended 2026-07-29 with Run SQL Backup + `rdm_postgres_config` (PLAN.md Phase 20.52, six more DECISIONS.md entries) after clarifying that SQL backup/restore shouldn't depend on `invenio-sql-backup.bash` being pre-installed. All previously-open items (OpenSearch bucket name, SQL restore load command, OpenSearch restore-conflict handling, Postgres container/DB naming) resolved. Phases 20.48, 20.49, and 20.52 implemented and unit-tested 2026-07-29; Phase 20.50 implemented and unit-tested 2026-08-18 (not yet real-AWS-verified); Phase 20.51 not yet implemented -- still gates the release. See DESIGN.md, "RDM Backup & Restore Domain", PLAN.md Phases 20.48-20.52.
  - [x] Phase 20.48 -- domain wiring: new "RDM Backup & Restore" domain picker entry, relocate Feature 11 (Archive SQL Backup to S3) out of Compute. Implemented and unit-tested 2026-07-29, `go build`/`vet`/`test -race`/`gofmt` clean. Not yet real-AWS-verified (pure UI wiring; Archive SQL's own behavior is unchanged from its already-verified Compute-menu version). Will need a small follow-up once Phase 20.52 lands: `rdmMenuItems` gains a fifth entry ("Run SQL Backup").
  - [x] Phase 20.49 -- Archive OpenSearch Snapshot to S3: snapshot via OpenSearch's native Snapshot API (local `fs` repo synced by clasm), scoped to current-version record/list indices + stats aggregates (~8GB target), one snapshot on EBS at a time, app-managed S3-side cleanup. Implemented and unit-tested 2026-07-29, test-first throughout, `go build`/`vet`/`test -race`/`gofmt` clean, wired into the RDM Backup & Restore menu (replacing its stub). **Real-AWS testing started 2026-07-29 against CaltechAUTHORS, blocked before a snapshot could be created:** repo registration failed (HTTP 500, most likely the still-pending `path.repo` retrofit) but clasm's own error message showed only curl's generic "exit status 22", not OpenSearch's actual error -- fixed test-first by switching every OpenSearch `curl` command to `--fail-with-body` and surfacing the captured response body in the error message; also clarified the cleanup-threshold prompt's wording (was ambiguous about local-directory vs S3). See DECISIONS.md, "Real bug: `curl -fsS` hid OpenSearch's own error body..." and "Clarify Archive OpenSearch Snapshot to S3's cleanup-threshold prompt wording". The improved error message then confirmed the root cause directly (path.repo empty), and while retrofitting it a **third real bug** surfaced: repo registration used the operator-typed host directory as `location` instead of the fixed container-internal path (`/usr/share/opensearch/backups`) the retrofit's bind mount actually maps to -- fixed test-first via a new `DefaultOpenSearchContainerRepoPath` constant. See DECISIONS.md, "Real bug: repo registration used the host directory as `location`...". **A fourth, more serious issue surfaced attempting the retrofit itself, 2026-07-29/30 -- not a clasm bug, an infrastructure gap in the RDM deployments' own `docker-services.yml`:** neither CaltechAUTHORS production nor CaltechDATA dev (`data.caltechlibrary.dev`) has a persistent volume for OpenSearch's `search` service data directory. Recreating that container (required to pick up `path.repo`) discards every index unconditionally. Confirmed live on CaltechDATA dev -- the retrofit attempt there wiped all indices, recovered same-session via `invenio rdm-records rebuild-index` + per-index `invenio index create` + `invenio communities rebuild-index` + generic `invenio index reindex --pid-type ...`/`invenio index run` (site confirmed working again). CaltechAUTHORS production: `path.repo` was added to `docker-services.yml` but reverted **before** any container recreate, via clasm's own read-only SSM access -- no data was lost, but production's real risk is unresolved: ~150K live records plus ~10 months of `stats-record-view-*`/`stats-file-download-*`/`events-stats-*` usage history that has **no source of truth outside OpenSearch** (unlike records, not rebuildable from Postgres). `~/WorkLab/rdm-opensearch-path-repo-retrofit.md` rewritten with a mandatory persistent-volume-migration step (via `docker cp`) before any future retrofit attempt on either instance, plus the recovery procedure that worked. **Both instances currently reverted to pre-retrofit config (no `path.repo` set on either).** **Real-AWS-verified end to end on CaltechDATA dev, 2026-08-12:** redid the retrofit there following the rewritten runbook's safe persistent-volume migration (`docker cp` the live 269MB OpenSearch data dir out, `chown 1000:1000`, added the persistent volume + backups mount + `path.repo` in one `docker-services.yml` edit, recreated `search` once -- confirmed via `_cat/indices?v` that all real indices, including `stats-record-view-*`, survived the recreate this time), then registered the snapshot repo. A new IAM role (`rdm-opensearch-backup`, RDM Repository Instance template: SSM + scoped S3 List/Get/Put/Delete on `opensearch-backups.library.caltech.edu`) was attached to the instance for S3 access. That surfaced a **fifth real bug**, unrelated to OpenSearch itself: `internal/workflow/resource_lists.go`'s `listRoles`/`listInstanceProfiles` each made a single un-paginated IAM call, silently dropping roles/profiles past the first page -- the newly-created role never appeared in "Select a role to attach" even though "Show Roles" (which already paginates, fixed 2026-07-23) saw it fine. Fixed test-first, same shape as the 2026-07-23 fix it had been missing. See DECISIONS.md, "Real bug: `listRoles`/`listInstanceProfiles` un-paginated, sibling to a fix already made elsewhere". With that fixed, ran Archive OpenSearch Snapshot against `data.caltechlibrary.dev` for the first time: created snapshot `rdm-20260812-205453`, synced 4 objects/725 bytes to `s3://opensearch-backups.library.caltech.edu/data.caltechlibrary.dev/opensearch-snapshots/rdm-20260812-205453/`, verified, deleted the local EBS-side snapshot -- full lifecycle confirmed working except the S3-side cleanup branch (untested; no old snapshots existed yet to trigger it). **Attempted to exercise S3-side cleanup same day, found a real timing constraint (not a bug):** `promptOpenSearchCleanupDays`'s validator deliberately rejects `0`/blank-is-skip, only accepting a positive integer >=1 day; `FilterOlderThan`'s cutoff is `now - days`, so the smallest valid threshold (1) only flags snapshots created more than 24 hours ago. Both this morning's `rdm-20260812-205453` and a same-day retry are under 24 hours old, so no valid input can exercise the cleanup branch same-day. Deferred -- revisit once a day or more has passed and a real snapshot is actually old enough. **Next step:** once that's exercised, repeat this entire sequence (persistent-volume migration, `path.repo` retrofit, IAM role, live test) against CaltechAUTHORS production -- deliberately not attempted yet, given production's much higher stakes (~150K records, ~10 months of irreplaceable usage-stats history). Same day, separately: Feature 11 (Archive SQL Backup to S3) and Phase 20.52 (Run SQL Backup) both re-confirmed working against CaltechAUTHORS and CaltechDATA. **Release held on the user's explicit call until Phases 20.50/20.51 (both restores) are built and verified** -- do not release with just 20.48/20.49/20.52, even once 20.49's cleanup branch is confirmed. See `agents/hand-off/2026-08-12T220000Z-clasm-opensearch-archive-real-aws-verified-caltechdata-dev.spmd` for the full session account. **CaltechAUTHORS production retrofit completed and real-AWS-verified, 2026-08-17:** followed the runbook's safe-migration procedure live, one command at a time -- `docker cp`'d the live 19GB data dir out (size-matched against the container's own `du -sh` before proceeding), edited `docker-services.yml` in one pass (persistent-data volume + backups mount + `path.repo`), validated via `docker compose config` before the recreate, recreated `search` exactly once in a maintenance window. Every real index survived, including `caltechauthors-rdmrecords-records-record-v7.0.0` at 147,438 docs (matches the ~150K estimate) plus all `stats-*`/`events-stats-*` history -- confirmed via `_cat/indices?v`. Snapshot repo `rdm_backup_repo` registered successfully, first attempt, no repeat of the earlier host-vs-container-path bug. See `agents/knowledge.db` observations 171 (`caltechauthors`)/172 (`clasm`) and `agents/hand-off/2026-08-17T231100Z-clasm-opensearch-retrofit-caltechauthors-production-verified.spmd`. **IAM role attached, then a second real bug found and fixed test-first, same day:** reused the existing `rdm-opensearch-backup` role (account-level, created 2026-08-12), associated it to CaltechAUTHORS (`DescribeInstances` confirmed `IamInstanceProfile` went from `null` to the role's ARN). First live run then hit an `AccessDenied` S3 sync failure (a prior attach attempt hadn't actually landed yet -- fixed by the same-day re-attach) and, more seriously, a snapshot that reported `SUCCESS` with `shards.total: 0` -- an entirely empty backup, because `CreateSnapshot`'s index patterns were built from the instance's Name tag (`newauthors`) instead of its Project tag (`caltechauthors`, the real index prefix). Fixed via `indexPrefix := cmp.Or(inst.Project, prefix)`, used only for the index-match patterns (S3 destination path unaffected, still uses Name) -- same fix shape as Phase 20.52's Postgres `db_name`/`db_user` bug. See DECISIONS.md, "Real bug: Archive OpenSearch Snapshot's index-match patterns used the Name tag, not the Project tag." Regression test confirmed failing before, passing after; `go build`/`vet`/`test ./... -race`/`gofmt -l` clean. **Re-run against CaltechAUTHORS confirmed both fixes worked, same day.** Snapshot `rdm-20260817-235349` reported `state: SUCCESS` with `shards: {"total":59,"failed":0,"successful":59}` (vs. `total:0` on the broken first attempt) -- real indices matched this time. Synced 2,976 objects / 6,425,452,659 bytes (~6.4GB) to `s3://opensearch-backups.library.caltech.edu/newauthors/opensearch-snapshots/rdm-20260817-235349/`, verified, EBS-side snapshot deleted -- the full lifecycle confirmed working end to end against CaltechAUTHORS production for the first time, with real data. Independently confirmed against the `--debug` JSONL log (not just the printed summary line): `CreateSnapshot` accepted, status polling `IN_PROGRESS` for ~3 minutes then `SUCCESS`, sync `Success` with no stderr, delete `Success`. **Next: CaltechDATA production (`data.caltech.edu`, distinct from the already-fixed `data.caltechlibrary.dev` dev instance) has not been touched at all -- needs the entire retrofit sequence from scratch; Phases 20.50/20.51 still gate the release.**
  - [x] Phase 20.50 -- Restore SQL Backup from S3: load command confirmed against the real `invenio-sql-backup.bash`/`invenio-sql-restore.bash` (drop-and-recreate via `docker exec`-wrapped `psql`, not `pg_restore`). Discovers-and-reconciles its own target's Postgres container live (shares Phase 20.52's helper) rather than assuming a naming convention. Implemented and unit-tested 2026-08-18, test-first throughout, `go build`/`vet`/`test -race`/`gofmt` all clean. Two implementation-time corrections, both documented in DECISIONS.md: Postgres-target discovery now runs before any S3 prompt (fail fast on a broken target); the DROP/CREATE DATABASE statements now SQL-identifier-quote the database name (found real, setting up `caltechdata-restore-test`, whose own database name `rdm14-granian` is hyphenated -- not a valid unquoted identifier). Not yet real-AWS-verified against this restore-test instance -- next step.
  - [ ] Phase 20.51 -- Restore OpenSearch Snapshot from S3: delete-then-restore for conflicting indices (decided 2026-07-29). Depends on Phase 20.49's primitives.
  - [x] Phase 20.52 -- Run SQL Backup + `rdm_postgres_config`: new on-demand "Run SQL Backup" action triggers `pg_dump` directly via SSM (no pre-installed script needed), chains into the existing Archive SQL Backup workflow on confirm. Postgres container discovered live every run via an unfiltered `docker ps` plus client-side image matching (survives naming-convention drift, e.g. the underscore-to-dash Docker Compose change already seen once) -- reconciled with a new `rdm_postgres_config` section in `~/.clasm` (editable via the Configure clasm domain, for deployments customized beyond Invenio RDM's shipped defaults). Implemented and unit-tested 2026-07-29, test-first throughout. **Four real bugs found via live testing against CaltechAUTHORS production the same day, all fixed test-first:** (1) Docker's own `--filter ancestor=postgres` doesn't match a tagged image (`postgres:14.13`) -- fixed by dropping the filter and matching images client-side instead; (2) `pg_dump | gzip` masked `pg_dump`'s own exit status (a failed dump still reported Success) -- fixed by matching `invenio-sql-backup.bash`'s real non-piped two-step structure (`redirect > file; gzip -f file`, joined by `set -e`); (3) `db_name`/`db_user` defaulted to the instance's Name tag, which for this real instance is a legacy label (`newauthors`) unrelated to its actual project (`caltechauthors`) -- fixed by preferring the Project tag over Name; (4) bug 3's own fix still didn't work at first, because `inventory.tagValues` matched the `Project` tag key by exact case and this instance's real tag is lowercase `project` -- fixed by matching case-insensitively at first, confirmed safe across the whole fleet (checked every instance across every local `--debug` log). **Real-AWS re-verification succeeded**: a correctly-sized (985MB), correctly-named backup landed, matching the pre-existing cron-produced backup for the same day. Follow-up same day: after confirming with a colleague that the team's real standard is lowercase `project`, the user retagged every clasm-tagged resource using capitalized `Project` (found via a live `resourcegroupstaggingapi` query: one AMI, two launch templates, one instance) and asked to remove the case-folding -- `tagValues` reverted to exact-match lowercase `"project"`, simpler code, a regression test locks in the reversion. Also same day: RDM Backup & Restore moved before Configuration in the domain picker (user's explicit call). `go build`/`vet`/`test -race`/`gofmt` clean throughout every change. **Run SQL Backup is now real-AWS-verified end to end.**

## Requested features

- [x] **Before the next release (user's target: 2026-08-18):** fix `PollSnapshotUntilComplete` (Archive OpenSearch Snapshot to S3, Phase 20.49) printing nothing while it waits for OpenSearch to finish -- the terminal just sits on whatever was last drawn (e.g. the bucket picker's `(/ filter, q to cancel)` hint), which can read as a hung prompt during what's actually a legitimate multi-minute wait on a large index set (confirmed live 2026-08-17 against CaltechAUTHORS production: ~3 minutes for a real ~6.4GB/59-shard snapshot, vs. the instant response of the earlier broken 0-shard case). Not a bug -- the workflow completes correctly either way -- but a real UX gap the user explicitly wants closed before shipping next, not deferred to someday/maybe. Fixed 2026-08-18 via a new shared `pollWithProgress` helper (prints an initial "waiting for ... to complete" message plus an elapsed-time line on each `pollInterval` tick; also feeds Phase 20.51's sibling poller for free), test-first, `go build`/`vet`/`test -race`/`gofmt` all clean. Not yet real-AWS-verified. See DESIGN.md, "Poll-Loop Progress Output: OpenSearch Snapshot/Restore Polling", DECISIONS.md same title, PLAN.md Phase 20.53.
- [x] Delete Role's "still referenced by instance profile(s)" refusal points at the wrong remedy. Found 2026-08-18 cleaning up the now-unused `rdm-opensearch-backup` role after rolling out the combined `rdm-backups` role (see `agents/knowledge.db` clasm observation id 180): `deleteIAMRoleConfirmed`'s `ReferencedByProfiles` check (`fetchIAMRoleDetail`, `internal/workflow/iam_detail.go`) is based on `iam:ListInstanceProfiles`' `Roles` membership -- whether the role is a member of an IAM instance-profile *object* -- not on whether any running EC2 instance actually has that instance profile attached. The error message tells the operator to "Detach it first (Compute domain, Associate/replace IAM instance profile)," but that action only changes the EC2-to-instance-profile *association* and has no effect on role-to-instance-profile *membership* -- so following its own suggested remedy cannot ever unblock this. clasm has no action anywhere for `iam:RemoveRoleFromInstanceProfile` or `iam:DeleteInstanceProfile` (confirmed via a full source grep, zero matches for either) -- had to work around it via the raw AWS CLI. Fix: (a) correct the message to name the actual blocker and remedy, and (b) add the missing capability, either as something Delete Role offers to do automatically (remove the role from any instance profile it's a member of, optionally delete now-empty instance profiles too) or as new standalone IAM-domain actions. **Fixed 2026-08-18** -- (a) reworded (both `deleteIAMRole`'s hard-refuse and `deleteIAMRoleConfirmed`'s pre-check), (b) as two new standalone IAM-domain actions ("Remove role from instance profile", "Delete instance profile", inserted after Detach Policy from Role and before Delete Role), not auto-cascaded from Delete Role. Test-first, `go build`/`vet`/`test -race`/`gofmt` all clean. Not yet real-AWS-verified. See DESIGN.md, "Delete Role's Wrong-Remedy Message + Missing Instance-Profile-Membership Capability", DECISIONS.md same title, PLAN.md Phase 20.55.
- [ ] Ad-hoc "run a shell command on an instance via SSM" action -- worth exploring, raised 2026-08-18 after a session's worth of debugging CaltechAUTHORS/CaltechDATA production by hand: `aws ssm send-command`, copy the returned `CommandId`, then a separate `aws ssm get-command-invocation --command-id <id> --instance-id <id>` call to see the actual result -- tedious and easy to fumble across many iterations (this session needed it repeatedly: checking `aws sts get-caller-identity` to watch an IAM instance-profile transition, restarting `amazon-ssm-agent`, etc.). clasm already has the underlying primitive: `RunShellCommand` (`internal/workflow/ssm.go`) sends a command, polls `GetCommandInvocation` until a terminal status, and returns `stdout`/status in one call -- it's just not exposed as an interactive, general-purpose action, only used internally by specific workflows (Run SQL Backup, Archive OpenSearch Snapshot, etc.) with their own fixed commands. A new Compute-domain (or a new "Debug"/"Diagnostics" grouping) action -- pick an instance, prompt for a shell command, run it via the existing `RunShellCommand` helper, print stdout/stderr/status directly -- would remove the manual send/copy-id/fetch dance entirely for ad-hoc debugging. Not scoped or designed yet; a real candidate for this project's usual design -> plan -> decision -> test -> code order before starting, including a decision on how destructive/arbitrary shell access should be gated (this is meaningfully different from every other SSM use in this codebase, which all run a small set of clasm-authored, reviewed commands, not arbitrary operator-typed ones).
- [x] Associate/replace IAM instance profile's picker can trap an operator in a confusing `EntityAlreadyExists` loop. Found 2026-08-18 while creating the `rdm-backups` role (see `agents/knowledge.db` clasm observation id 177): if the operator doesn't explicitly filter/select an already-existing profile from the picker (easy to miss among unrelated profiles, e.g. SageMaker service-catalog defaults), picking "Create new instance profile" instead re-triggers `create_instance_profile.go`'s flow, whose "New instance profile name" prompt defaults to the role's own name via `ui.WithDefault(role.Name)` -- exactly the name that already exists in a case like this one. `createInstanceProfileForRole` handles the resulting `EntityAlreadyExists` by looping and re-prompting for a *different* name rather than offering "use the existing one," and cancelling that prompt (Esc/Ctrl+C) aborts the whole Associate/replace workflow instead of returning to the profile picker -- recovery required a full clasm restart. Fix, one or more of: (a) default-select or flag an existing profile that exact-name-matches the role just picked in the create-new sub-flow; (b) have the `EntityAlreadyExists` handler offer "use the existing one" alongside "type a different name"; (c) make cancelling the name prompt return to the outer picker instead of aborting the whole workflow, matching the `cancelledIsNil` recoverable-cancel pattern already used elsewhere in this codebase. **Fixed 2026-08-18** -- (b) and (c) both, (a) dropped as subsumed by (b); "use existing" returns the resolved name for actual use (`created=true`, corrected from an initial `created=false` sketch after tracing the caller). Test-first, `go build`/`vet`/`test -race`/`gofmt` all clean. Not yet real-AWS-verified. See DESIGN.md, "Associate/Replace IAM Instance Profile: Recoverable `EntityAlreadyExists`", DECISIONS.md same title, PLAN.md Phase 20.56.

## Nice to have

- [ ] `checkCloudInitCompletion` (`cloud_init_check.go`) collapses cloud-init's own `--wait` exit code to a flat "done" or "error, check the instance before using it" -- but cloud-init distinguishes exit 0 (clean) from exit 2 ("degraded": completed, but at least one module reported a non-fatal `recoverable_errors` warning). Found 2026-08-18 launching a fresh dev/restore-test instance (`caltechdata-restore-test`, `i-0f2483bb0885836e5`) from `granian-rdm-v14/cloud-init.yaml`: clasm reported "cloud-init reported an error -- check the instance before using it" for what turned out to be two entirely harmless `groups:` warnings ("Skipping creation of existing group 'staff'/'www-data'", both pre-existing on stock Ubuntu) -- `errors: []` was empty the whole time. Required a full SSM session + `cloud-init status --long` to confirm it was a false alarm rather than a real failure. Fix: have `CloudInitCheckResult` distinguish "degraded" (exit 2, only `recoverable_errors`) from a hard error (exit 1, or anything in `errors`), and surface the actual `recoverable_errors`/`errors` text in the reported message instead of the generic "check the instance" -- same "don't hide the real error behind a generic message" lesson as the 2026-07-29 `curl -fsS`/OpenSearch fix (DECISIONS.md, "Real bug: `curl -fsS` hid OpenSearch's own error body..."). Not scoped or designed yet.
- [ ] A means of reporting the details of our an EC2 Instance, AMI instance or launch template, that can be downloaded as Markdown report document
- [ ] Be able to list the CloudFront distribution ID for S3 static websites
- [ ] Bulk object delete (the file manager's Delete/Sync actions, `internal/filemanager`) currently loops one `s3:DeleteObject` call per key. `github.com/peak/s5cmd/v2`'s `storage.S3.MultiDelete` batches keys into groups of up to 1000 and calls the batch `s3:DeleteObjects` API in parallel chunks -- not importable directly (aws-sdk-go v1 + urfave/cli coupling, vs. this project's aws-sdk-go-v2), but `aws-sdk-go-v2`'s `s3` package already exposes `DeleteObjects`, so the same batching pattern could be reimplemented natively without a new dependency. Evaluated 2026-07-09, flagged again as an open question in PLAN.md Phase 20.1's work items, still not started.
- [ ] Retry-on-launch-failure (general case): instead of bouncing back to
      the main menu on any `RunInstances` error, keep the already-collected
      params and let the operator re-enter just the field that's likely
      wrong instead of re-doing the whole launch flow. Granularity not yet
      decided (just Instance type, or any collected field). NOTE: the
      instance-type/AZ pre-flight check above already does a scoped version
      of this (change instance type / pick a different subnet / abort) for
      that one failure class, pre-flight rather than reactive -- this item
      is about generalizing to *any* RunInstances failure, not just that one.


## Someday/maybe (not on the active roadmap)

- Container management: AWS container registry support as a top-level
  menu item, plus SSM-based interaction with Docker containers running
  inside an EC2 instance (list the Docker services on an instance;
  image a running Docker service and save that image to the private
  container registry or S3). Moved here from Requested Features
  2026-07-20 -- no design work done, no committed timeline. The two
  items are grouped together (not split back into two separate
  entries) since the Docker-service imaging workflow's own save target
  is the container registry this would introduce -- picking this back
  up means designing both together, not the registry alone.

- CloudFront domain (PLAN.md Phase 21): designed in DESIGN.md/PLAN.md,
  no code written yet. Postponed by the user (2026-07-09), then further
  demoted the same day from "postponed to a later version" to
  someday/maybe -- no committed timeline (see DECISIONS.md, "Demote
  CloudFront to someday/maybe..."). Removed from `DomainActions`/the
  domain picker rather than left as a "not yet implemented" placeholder
  entry, so 0.0.1 doesn't expose a menu item that goes nowhere. The
  design in DESIGN.md/PLAN.md stays valid reference for if this is ever
  picked back up. Phase 22 (real-AWS testing for Key Management/S3) no
  longer depends on it.

- A compliance/audit-style report across the Tag Management domain's
  five resource types (EC2 instance, AMI, launch template, key pair, S3
  bucket): which resources are missing tags entirely (or missing the
  Project/Environment convention specifically). Raised 2026-07-20
  during Tag Management's design, expected to come up again later --
  distinct from "Show all tags" (Phase 20.30, per-resource-type,
  showing what each resource *has*), this would be a cross-type view
  answering "what's missing," which is a different query shape and
  likely needs its own design pass (e.g., does "missing tags" mean zero
  tags at all, or specifically missing Project/Environment?). Not
  scoped or started.

